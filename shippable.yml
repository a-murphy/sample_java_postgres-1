language: java

jdk:
 - openjdk7
 
build:
  ci:
   - ls ~/.gradle/caches
   - ./gradlew  --build-cache clean build
   - ./gradlew --build-cache test
   - ./gradlew --build-cache cobertura
   - ls ~/.gradle/caches
  cache: true
  cache_dir_list:
   - ~/.gradle/caches/build-cache-*

