language: java

jdk:
 - openjdk7
 
build:
  ci:
 #  - ls .gradle/caches
   - gradle clean build --build-cache
   - gradle test
   - gradle cobertura
   - ls .gradle
  cache: true
  cache_dir_list:
   - .gradle/caches/build-cache-*

