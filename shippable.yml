language: java

jdk:
 - openjdk7
 
build:
  ci:
 #  - ls .gradle/caches
   - git config --list
   - gradle clean build
   - gradle test
   - gradle cobertura
   - ls .gradle/caches
  cache: true
  cache_dir_list:
   - $(gradle --project-cache-dir)/caches/build-cache-*

