language: java

jdk:
 - openjdk7
 
build:
  ci:
 #  - ls .gradle/caches
   - gradle clean build --build-cache
   - gradle test
   - gradle cobertura
   - gradle --project-cache-dir
  cache: true
  cache_dir_list:
   - $(gradle --project-cache-dir)/caches/build-cache-*

