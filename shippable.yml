language: java

jdk:
 - openjdk7
 
build:
  ci:
 #  - ls .gradle/caches
   - gradle  --build-cache clean build
   - gradle test
   - gradle cobertura
   - ls .gradle
  cache: true
  cache_dir_list:
   - $SHIPPABLE_BUILD_DIR/.gradle/caches/build-cache-*

