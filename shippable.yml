language: java

jdk:
 - openjdk7
 
build:
  ci:
   - ls ~/.gradle/caches
   - ./gradlew  --build-cache clean build
   - ./gradlew test
   - ./gradlew cobertura
   - ls ~/.gradle/cahces
  cache: true
  cache_dir_list:
   - ~/.gradle/caches/build-cache-*

