language: java

jdk:
 - openjdk7
 
build:
  ci:
 #  - ls .gradle/caches
   - ./gradlew  --build-cache clean build
   - ./gradlew test
   - ./gradlew cobertura
   - ls .gradle
  cache: true
  cache_dir_list:
   - $SHIPPABLE_BUILD_DIR/.gradle/caches/build-cache-*

