language: java

jdk:
 - openjdk7
 
build:
  ci:
   - ls $GRADLE_HOME/caches
   - git config --list
   - gradle clean build
   - gradle test
   - gradle cobertura
   - ls $GRADLE_HOME/caches
 cache: true
 cache_dir_list:
   - $GRADLE_HOME/caches/build-cache-*

